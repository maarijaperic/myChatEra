OPENAI_API_KEY=sk-your-api-key-here
PORT=8000

# Set to "true" to enable fake version (file import login) for App Store review
# Set to "false" or leave unset to use real version (web view login)
USE_FAKE_VERSION=false

